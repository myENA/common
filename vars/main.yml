---
# vars file for stack-common/
slack_token: 
slack_notify: 
root_password: "{{ lookup('password', 'credentials/randomroot length=15') }}"
ntp_enabled: true
ntp_timezone: Etc/UTC
ntp_manage_config: true
ntp_servers:
  - "207.191.191.90"
